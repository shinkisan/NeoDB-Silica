/**
 * Fixed GitHub links for the "built on NeoDB and NeoDB Silica" credit line
 * (About page, profile page footer). These point at the upstream projects
 * themselves, not at a given deployment, so — unlike `site.config.ts` — they
 * are not meant to be edited per deployment.
 */
export const NEODB_GITHUB_URL = "https://github.com/neodb-social/neodb";
// TODO: replace with the real repo URL once it's public.
export const SILICA_GITHUB_URL = "#";
